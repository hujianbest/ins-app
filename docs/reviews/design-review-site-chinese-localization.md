# 设计评审记录：网站全站中文化

- **评审对象：** `docs/designs/2026-04-08-site-chinese-localization-design.md`
- **评审技能：** `ahe-design-review`
- **评审日期：** 2026-04-08
- **对照规格：** `docs/specs/2026-04-08-site-chinese-localization-srs.md`（以 `task-progress.md` 与人类已确认口径为已批准输入；规格文件头部「状态: 草稿」为文档卫生问题，建议父会话在规格定稿后同步状态字段）

## 结论

通过

## 发现项

- [minor] **规格评审遗留的边界清单仍未设计层固化**：`spec-review-site-chinese-localization.md` 建议在设计中用页面/表层清单显式映射全局错误页、HTTP 错误态、第三方组件内置英文等纳入或排除。当前设计 §12.1 有原则性说明（含「未显式打开的错误页或法务页」），但尚未形成可勾选对照表；不阻塞任务规划，建议在 `ahe-tasks` 或设计小修订中补一行「范围核对表」以免实现争议。
- [important] **核心术语在术语表中仍保留双选，需在人类确认或首任务中收敛为单一表达**：§9.2 / §12.2 对 `studio`（创作台 / 工作台）、`profile`（主页 / 个人主页）仍为择一待定；与 `NFR-001`「关键术语一致」一致，但批准前应在真人确认或任务计划首条明确最终用词，避免实现期漂移。不构成架构或方案层面的阻塞，因设计已显式标注为待定并给出收敛时机。
- [minor] **FR-005 子页面 metadata 覆盖策略未逐路由枚举**：§7.1 明确根布局 metadata 与 `lang`；若各 `page.tsx` / `layout` 存在独立 `generateMetadata` 英文残留，设计未逐页列出。与「按模块清单 + rg 排查」缓解策略一致，任务层可按路由清单收口即可。
- [minor] **设计文档头部仍为「草稿」**：与评审结论「可通过」并行；真人确认设计后应由父会话将设计状态与规格头部状态一并对齐，避免后续追溯误读。

## 薄弱或缺失的设计点

- 无足以让任务规划建立在错误前提上的空洞；文案来源视图（§6.1）、实施边界（§6.2）、浏览器验证路径（§8.3、§11.3）与规格 `FR-001`～`FR-005`、`NFR-001`～`NFR-003` 映射表（§3）已形成可追溯闭环。
- 内部分项评分（辅助，非结论本身）：需求覆盖与追溯约 8.5/10；架构一致性约 9/10；决策质量约 9/10；接口与任务规划准备度约 8/10（术语双选待收敛）；测试准备度与风险可控性约 9/10。

## 下一步

- `通过`：`设计真人确认`（人类批准设计定稿后，下一技能为 `ahe-tasks`）

## 记录位置

- `docs/reviews/design-review-site-chinese-localization.md`

## 交接说明

- **设计真人确认：** 结论为 `通过`；未经真人批准不得进入 `ahe-tasks`（AHE 硬门禁）。
- **ahe-design：** 若真人要求修订术语表固化、范围核对表或 metadata 枚举深度，可回修设计后再评审。
- **ahe-workflow-router：** 不适用（未发现需求漂移或 route/stage/证据链冲突）。
