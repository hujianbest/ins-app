# UI 决策矩阵模板

本模板用于写 `## 5. 候选视觉 / 交互方向与对比` 的 compare view，确保 reviewer 可冷读 trade-off。

## 使用原则

- 至少 2 个真实可行候选方向，禁止"推荐方向 + 稻草人"
- 若复用既有 Design System 是选项之一，也必须作为候选方向进入矩阵
- 每个候选方向都要填完整个矩阵（不留空），否则视为未完成比较
- `主要风险` 与 `可逆性` 必须显式，不能只写"低"或"小"
- `NFR / 约束匹配` 逐项承接规格的可用性 / a11y / i18n / 响应式 / 性能预算要求，缺任一视为未完成比较

## 默认矩阵

```markdown
| 维度 | 方向 A | 方向 B | 方向 C（可选） |
|------|--------|--------|----------------|
| 风格主张 | 一句话可冷读 | 一句话可冷读 | ... |
| Typography | 显示字体 + 正文字体组合（含 fallback） | ... | ... |
| 色彩策略 | 主/功能/语义色 + 亮暗主题策略 | ... | ... |
| 空间与节奏 | 间距 scale、布局密度、栅格基准 | ... | ... |
| 动效策略 | 使用/不使用、时长、缓动、reduced-motion | ... | ... |
| 组件来源 | 复用 / 扩展 / 新增哪套 Design System | ... | ... |
| 响应式策略 | 断点、内容优先级 | ... | ... |
| i18n 适配 | 多语种排版、RTL 支持 | ... | ... |
| 性能预算匹配 | LCP / INP / CLS / 首屏字节预算是否可达 | ... | ... |
| a11y 风险 | 预期阻塞点（对比度、焦点、语义） | ... | ... |
| 主要收益 | 具体到可冷读的 2-3 条 | ... | ... |
| 主要风险 | 具体到可冷读的 2-3 条 | ... | ... |
| 引入成本 | 学习曲线、依赖规模、团队熟悉度 | ... | ... |
| 可逆性 | 高 / 中 / 低 + 一句话理由 | ... | ... |
```

## 关键决策类型（按需各自做矩阵）

不是所有决策都要一张 6x10 大矩阵。下列决策按影响面各自写一份紧凑矩阵即可：

### 1. 视觉方向决策

定位、气质、typography / color / spacing / motion 的总基调。

### 2. 组件库 / Design System 选型

例：复用既有内部 DS vs 引入 shadcn/ui vs 引入 Ant Design vs 自建。

对比维度至少：a11y 成熟度、主题能力、组件覆盖、bundle 体积、生态活跃度、与既有栈的融合成本。

### 3. 导航范式

例：top-nav vs side-rail vs hybrid vs command palette first。

对比维度至少：信息密度、可扩展性（未来功能加入成本）、移动端适配、发现性、对主用户任务的路径长度。

### 4. 布局范式

例：dashboard 网格 vs 单列聚焦 vs 主次分栏 vs 全屏画布。

### 5. 表单模式（若规格含重表单）

例：单页长表单 vs 分步向导 vs 侧抽屉 vs 即编即保存。

### 6. 数据呈现模式（若规格含列表/表格）

例：传统分页表 vs 无限滚动 vs 游标分页 + 虚拟列表 vs 卡片流。

## 与 ADR 的衔接

每个关键决策选定后，用 `../../hf-design/references/adr-template.md` 的 ADR 格式记录。ADR 要求：

- 被考虑的备选方案与否决理由来自本矩阵
- 可逆性评估与矩阵中的"可逆性"列口径一致
- 后果（正面 / 负面 / 中性）必须显式，不能只写"会更好用"

## 冷读测试

写完后做一次自检：让一个未参与讨论的 reviewer 只读矩阵 + ADR，能否回答：

1. 为什么没选方向 B？
2. 选定方向的最大代价是什么？
3. 哪些 NFR 是选型的决定性因素？
4. 如果半年后要换方向，成本是什么量级？

四题都不能冷读出 → 矩阵不合格，继续补。
